# Pre-built release binaries - download from GitHub releases instead
releases/
logs/

# Local deployment credentials — copy .env.example to .env and fill in
.env

# Mobile Control UI build outputs (source is tracked)
mobile-control-ui/node_modules/
mobile-control-ui/dist/
mobile-control-ui/dist-app/
